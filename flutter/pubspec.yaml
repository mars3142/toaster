name: toaster
description: Toaster records every message.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+10000

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: '>=3.13.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  auto_route: ^7.8.3
  cupertino_icons: ^1.0.2
  hooks_riverpod: ^2.4.0
  intl: ^0.18.1
  riverpod_context: ^0.3.0
  dynamic_color: ^1.6.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  auto_route_generator: ^7.3.1
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
  generate: true
flutter_intl:
  enabled: true
